import type { Animation } from "@lottiefiles/lottie-types";

import json from "./mockJson.json";

const mockPlaygroundData = {
  id: "123",
  json: json as Animation,
};

export default mockPlaygroundData;
